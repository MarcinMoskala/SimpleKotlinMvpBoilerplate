package ${packageName};

import ${applicationPackage}.presentation.common.Presenter

class ${activityClass}Presenter(val view: ${activityClass}View): Presenter() {

    override fun onStart() {
    }
}